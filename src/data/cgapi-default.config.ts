import type { CgapiConfigType } from '../infrastructure/types/cgapi.types.js'

const config = {
  isPostman: false,
  configPath: 'codegen/',
  type: 'rtk-query',
} as CgapiConfigType

export default config
